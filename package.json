{
  "name": "thechefos-workers",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "deploy:router": "cd packages/router && wrangler deploy",
    "deploy:gateway": "cd packages/ai-gateway && wrangler deploy",
    "dry-run:router": "cd packages/router && wrangler deploy --dry-run",
    "dry-run:gateway": "cd packages/ai-gateway && wrangler deploy --dry-run",
    "deploy:telegram-bot": "cd packages/telegram-bot && wrangler deploy",
    "dry-run:telegram-bot": "cd packages/telegram-bot && wrangler deploy --dry-run"
  }
}
